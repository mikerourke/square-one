import styled from 'styled-components';

const Header = styled.div`
    display: flex;
    align-items: center;
`;

const HeaderText = styled.span`
    font-size: 24px;
    padding-left: 16px;
`;

export {
    Header,
    HeaderText,
};
